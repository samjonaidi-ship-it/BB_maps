{
  "name": "bb-maps",
  "version": "1.0.0",
  "description": "BB Maps — self-hosted map tile serving, Photon geocoder, Esri satellite proxy",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "lint": "eslint src --max-warnings 0",
    "test": "node --test src/**/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "@fastify/cors": "^11.0.0",
    "@fastify/rate-limit": "^10.2.0",
    "undici": "^7.8.0",
    "pmtiles": "^4.3.0",
    "pino": "^9.7.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "@eslint/js": "^9.0.0"
  }
}
